import { localStorageUtil } from '@utils';

export const Home = () => {
	//  localStorage.clear()
	// console.log(localStorageUtil.getAll());

	return (
		<>
			<div>Test localStorage</div>
		</>
	);
};

export default Home;
